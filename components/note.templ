package components

import (
	"fmt"
	"time"

	"github.com/yulog/mi-diary/model"
)

// SQLite は日時をUTCで保持する
templ noteList(items []model.DisplayNote) {
	<ul class="columns is-multiline">
		for _, item := range items {
			<li class="column is-full">
				<div class="card">
					<div class="card-content">
						<div class="media">
							<div class="media-content">
								<p>
									{ item.DisplayName }
								</p>
								<p>
									{ fmt.Sprintf("@%s", item.UserName) }
								</p>
							</div>
						</div>
						<div>
							{ item.Text }
						</div>
						<a href={ templ.URL(fmt.Sprintf("https://misskey.io/notes/%s", item.ID)) } target="_blank">
							{ item.ID } ({ item.CreatedAt.In(time.Local).Format(time.DateTime) })
						</a>
					</div>
				</div>
			</li>
		}
	</ul>
}

type Note struct {
	Title   string
	Profile string
	Items   []model.DisplayNote
}

templ (n Note) WithPage() {
	@BaseWithNav(n.Title, n.Profile) {
		@noteList(n.Items)
	}
}

templ (n Note) WithPages(p Pages) {
	@Pagination(n.Title, n.Profile, p) {
		@noteList(n.Items)
	}
}
